name: example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  re_highlight: ^0.0.3
  google_fonts: ^8.0.1
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  code_forge: { path: ../ }
  intl: ^0.20.2

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
